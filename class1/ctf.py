# coding: utf-8
import hashlib
import strings
import string
f=file('findtheflag.txt', 'r')
flags = [flag.string() for flag in f]
flags = [flag.strip() for flag in f]
flags[0]
string.ascii_letters
letters = [_.split()]
letters
string.ascii_letters
letters = [_.split('')]
_.split()
letters = [l for l in string.ascii_letters]
letters
len(flags)
for f in flags:
    
    
    print hi
    
flag = []
for f in flags:
    
    
    fsddf
    
for f in flags:
    for l in letters:
        hex = hashlib.sha256(l).hexdigest()
        if(f == hex):
            print l
        else:
            print 0
            
for f in flags:
    for l in letters:
        hex = hashlib.sha256(l).hexdigest()
        if(f == hex):
            flag.append(l)
        else:
            flag.append(0)
            
flag
for f in flags:
    for l in letters:
        hex = hashlib.sha256(l).hexdigest()
        if(f == hex):
            flag.append(l)
            
flag = []
flag
for f in flags:
    for l in letters:
        hex = hashlib.sha256(l).hexdigest()
        if(f == hex):
            flag.append(l)
            
flag
flag.join()
string.join(flag)
_.find('flag')
_.find('Flag')
flag = string.join(flag)
flag
flag.find('Flag')
len(flag)
len(flags)
for f in flags:
    for l in letters:
        hex = hashlib.sha256(l).hexdigest()
        if(f == hex):
            flag.append(l)
   continue
for f in flags:
    for l in letters:
        hex = hashlib.sha256(l).hexdigest()
        if(f == hex):
            flag.append(l)
   continue
for f in flags:
    for l in letters:
        hex = hashlib.sha256(l).hexdigest()
        if(f == hex):
            flag.append(l)
            continue
        
flag2 = []
for f in flags:
    for l in letters:
        hex = hashlib.sha256(l).hexdigest()
        if(f == hex):
            flag.append(l)
            continue
for f in flags:
    for l in letters:
        hex = hashlib.sha256(l).hexdigest()
        if(f == hex):
            flag.append(l)
            continue
        
for f in flags:
    for l in letters:
        hex = hashlib.sha256(l).hexdigest()
        if(f == hex):
            flag.append(l)
            continue
for f in flags:
    for l in letters:
        hex = hashlib.sha256(l).hexdigest()
        if(f == hex):
            flag.append(l)
         
for f in flags:
    for l in letters:
        hex = hashlib.sha256(l).hexdigest()
        if(f == hex):
            flag2.append(l)
            continue
        
flag2
flag2s = flag2.join()
flag2s = string.join(flag2)
flag2s
len(flag2s)
len(flag)
flag2s
%notebook -e ctf.ipynb
%save ctf.py
%save
%savectf.py
%save ctf.py
%save -r ctf.py
%save -a ctf.py
%save -a ./ctf.py
%save ctf
'%save ctf'
%save
%save "ctf.py"
%save -r ctf.py
%save -r ctf.py 1-9999
